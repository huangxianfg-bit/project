server:
  port: 8080


spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bigdatatest
    username: root
    password: ok

  devtools:
    remote:
      restart:
        enabled: true
    restart:
      additional-paths: Example_springboot/src/main/java
      exclude: static/**


mybatis:
  mapper-locations: classpath:mapper/*.xml

logging:
  level:
    org.example.dao: debug